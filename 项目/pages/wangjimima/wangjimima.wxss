/* 顶部白色矩形样式 */
.top-bar {
  width: 100%;
  height: 100px; /* 高度可以根据需要调整 */
  background-color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
}

.back-button {
  position: absolute;
  left: 20px;
  top: 55px;
  color: black;
  font-size: 18px;
}

.title {
  position: absolute;
  top: 55px;
  color: black;
  font-size: 20px;
}

/* 页面内容区域背景色 */
.content {
  background-color: #f5f5f5;
  height: calc(100vh - 60px); /* 减去顶部矩形的高度 */
  padding-top: 100px;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 手机号码输入区域 */
.input-section {
  display: flex;
  flex-direction: column;
  margin-bottom: 100px;
  width: 80%; /* 控制整体宽度 */
}

.label {
  color: black;
  font-size: 16px;
  margin-bottom: 10px; /* 控制输入框上方文字与输入框的距离 */
}

.phone-input-container {
  display: flex; /* 保持在同一行 */
  justify-content: space-between; /* 让它们之间的空间分配合理 */
}

.phone-input {
  flex: 3; /* 输入框占据较多空间 */
  border: 1px solid black;
  padding: 10px;
  border-radius: 4px;
  background-color: white;
}

.verify-button {
  flex: 1; /* 获取验证码按钮占据较小空间 */
  border: 1px solid black;
  padding: 10px;
  border-radius: 4px;
  background-color: white;
  color: black;
  margin-left: 10px; /* 按钮与输入框之间的间距 */
  font-size: 11px;
  white-space: nowrap;
}

/* 下一步按钮 */
.next-button {
  width: 80%;
  padding: 10px;
  background-color: #007aff;
  color: white;
  text-align: center;
  border-radius: 4px;
  font-size: 18px;
}